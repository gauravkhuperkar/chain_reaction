1.make tile entity
		[position,owner,hits]
		-tests
	-tile prototype
		> isCorner
		> isSide
		> hitCount
		> isBlast
		> capacity

2.make game entity
		[players,tiles]
		-tests
	-make game board (contain all tiles)
		[all tiles,usedTiles,emptyTiles, some properties]
	-game prototype
		> isSterted
		> isEnd
		> currentPlayer

3.make player entity
		[name, color,tileOwend]
		-tests
	-player prototype