1.make tile entity
		[position,owner,hits]
		-tests
	-tile prototype
		> isCorner
		> isSide
		> hitCount
			--> should not count the hits once the capacity of tile get fullfill
			--> should not allow to hit tile to opponent/opposite player
		> isBlast
			--> should not be check who is owner but should be change the owner of tile
		> capacity

2.make game entity
		[players,tiles]
		-tests
	-make game board (contain all tiles)
		[all tiles,usedTiles,emptyTiles, some properties]
	-game prototype
		> isSterted
		> isEnd
		> currentPlayer

3.make player entity
		[name, color,tileOwend]
		-tests
	-player prototype